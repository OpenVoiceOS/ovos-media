that device can't be controlled from here — try again from the device itself
this session can't control that player — control it directly from the device instead
remote control isn't allowed for that device — control it directly instead
